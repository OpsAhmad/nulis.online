{
  "name": "nulis.online-workspace",
  "version": "1.0.0",
  "description": "Monochrome Single Page Application for writing articles.",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n \"backend,frontend\" -c \"zinc,white\" \"cd backend && php artisan serve\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
